<h1>Tags</h1>
<ul id="tags">
<% @tags.each do |tag| %>
  <li>
    <%= link_to tag.name, tag_path(tag) %>
    <%= button_to 'Delete Tag "' + tag.name + '"', tag_path(tag), method: :delete %>
    <br>
  </li>
<% end %>
<%= link_to "Back to Articles", articles_path %>
</ul>
